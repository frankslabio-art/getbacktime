# Copy this to .env (gitignored) and fill in real values
# Never commit the actual .env file

VPS_HOST=YOUR_VPS_IP
VPS_USER=root
VPS_PORT=22
VPS_SSH_KEY=~/.ssh/id_ed25519

# App secrets (fill in on the server, not here)
DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
